with open('HISTORICO_DEL_PROYECTO.md', 'a', encoding='utf-8') as f:
    f.write('\n* **[2026-06-22] Landing_GTM_v2 (Fixes) - Reparación de Modo Oscuro, Permisos AR y Cola de Telemetría**:\n')
    f.write('    - **Dark Mode Global y Header Dinámico**: Restauración de la clase `dark` en la raíz de `layout.tsx` para hacer cumplir los Brand Guidelines (Obsidian Teal). Se parcheó `Header.tsx` con colores translúcidos (`bg-[#0a0a0a]/90`) y filtros de logo invertidos (`brightness-0 invert`) para legibilidad sobre el carrusel y en scroll.\n')
    f.write('    - **Violaciones de Política de Iframe (AR)**: Solución de advertencia `[Violation] Permissions policy violation: xr-spatial-tracking` inyectando los atributos `allow="xr-spatial-tracking; fullscreen; autoplay; web-share"` en el `LiveDemo.tsx` que aloja el Model Viewer.\n')
    f.write('    - **Cola de Eventos de Telemetría (Evitar 400)**: Corrección de error `400 Bad Request` en `telemetria_manuales`. Se implementó un `eventQueue` en `useTelemetry.js` (legacy-aplicativo-armado) para atrapar eventos prematuros (ej: tracking de paso 1 al montar) y retenerlos en memoria hasta que Supabase resuelva el `proyecto_id`.\n')
    f.write('    - **Navegación Fluida Landing-Plataforma**: Ajuste de enlaces del `Footer.tsx` (`Aviso de Privacidad` y `Términos`) apuntando a la plataforma B2B con soporte `window.history.back()` nativo.\n')
